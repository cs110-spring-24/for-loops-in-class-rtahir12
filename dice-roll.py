
# Simulate 100 dice rolls
for _ in range(100):
    roll_result = random.randint(1, 6)  # Roll a dice (1-6)
    dice_rolls.(roll_result)  # Store the result
    roll_counts[roll_result] += 1  # Count the occurrence

# Output the results
dice_rolls, roll_counts
